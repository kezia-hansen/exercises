"use strict";

const first = "Peter";

const second = "Antoinette";

let result = first.substring(0, 1).toUpperCase(0, 1) + first.substring(1).toLowerCase(1);
console.log(result);

let result2 = second.substring(0, 1).toUpperCase(0, 1) + second.substring(1).toLowerCase(1);
console.log(result2);
